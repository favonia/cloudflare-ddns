😦 IP4_DOMAINS accepts ".accepted.example" for now; use accepted.example instead because version 2.0.0 will reject the current spelling
😡 IP4_DOMAINS has consecutive dots in "fatal..empty.example"; replace each run with a single dot
